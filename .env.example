# Database
# For local development, use a local Postgres instance
# For Vercel deployment, this will be automatically set by Vercel Postgres
# When using Prisma Accelerate, set DATABASE_URL to the Accelerate connection string (prisma://...)
# and DIRECT_DATABASE_URL to the direct database connection for migrations
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kanban?schema=public"
DIRECT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kanban?schema=public"
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""

# Session
SESSION_SECRET="change-this-to-a-random-string-in-production"
